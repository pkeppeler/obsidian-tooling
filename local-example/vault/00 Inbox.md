# 00 Inbox

Dump anything here — unstructured is fine, from phone, desktop, or Claude.
`/triage` routes each item to its permanent home. Optional capture tags
(`#next`, `#p1`/`#p2`/`#p3`, `#waiting`) are documented in `CLAUDE.md`.

- Try the tooling: add an item to this file, then run `/triage` in Claude Code
- [x] Sample completed inbox item — will be archived in the pre-triage pass
