# 00 Dashboard

Reading-mode (⌘E) view of active work. Tasks-plugin queries surface tasks
from across the vault, grouped by their source file and heading.

## ➡️ Next actions

```tasks
not done
(tags include #next) OR (tags include #p1)
group by filename
group by heading
sort by tag
```

## 🔺 All P1

```tasks
not done
tags include #p1
group by filename
group by heading
```

## 🔥 In-flight projects

<!-- Curated list of [[wikilinks]] to active projects. Maintain by hand during /triage. -->

## P2

```tasks
not done
tags include #p2
group by filename
group by heading
```

## ⏸ Waiting on

```tasks
not done
tags include #waiting
group by filename
group by heading
```
